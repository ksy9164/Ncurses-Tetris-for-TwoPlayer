이 코드는 우분투 16.04에서 실행 했을때 오류가 없었습니다.

설명:
C코드는 총 9개, Deadlock의 3가지 버전 (일반Deadlock,prevent,avoid) 이 각 3개의 철학자로 있습니다.
즉, 각 버전별로 코드를 확인하고 싶으시다면, deadlockPA.c,  preventionPA.c , avoidPA.c 이렇게 3개만 확인 하시면 됩니다.

실행:
start로 시작하는 sh 파일을 실행하면, 자동 컴파일&실행이 되고,실행이 완료되면 자동적으로 실행파일을 지우며, 세마포 또한 지웁니다 (강제 종료하지 않을시)
강제 종료할 시를 대비해서, clean 이라는 sh파일도 넣었습니다.
코드를 실행한후 강제종료시 다른 쉘에서 ./clean 을 실행하면, 실행파일을 지우며,세마포를 지워서 강제 종료 가능합니다. 
혹시, sh 파일이 실행이 안된다면, chmod를 통해 권한설정을 바꿔주시면 가능합니다.

ex)
./startDeadlock.sh
일반 deadlock을 실행하고 싶다면, 이렇게 실행하면 가능합니다.
